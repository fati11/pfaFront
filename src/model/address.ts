export interface Address {
    // tslint:disable-next-line:indent
    address: string;
    // tslint:disable-next-line:indent
    city: string;
    state: string;
    country: string;
    zipcode: string;
    phonenumber: string;
}
